Flask>=2.3.3
cryptography>=40.0.0
pqcrypto>=0.3.4
asn1crypto>=1.5.0
click>=8.1.0
colorama>=0.4.6
tabulate>=0.9.0
pyyaml>=6.0.0
Werkzeug>=2.3.7
Jinja2>=3.1.2
itsdangerous>=2.1.2
MarkupSafe>=2.1.3
blinker>=1.6.3

# Database support
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0  # PostgreSQL adapter
PyMySQL>=1.1.0  # MySQL adapter

# API and documentation
Flask-RESTful>=0.3.10
flask-marshmallow>=0.15.0
marshmallow>=3.20.0
marshmallow-sqlalchemy>=0.29.0
flasgger>=0.9.7  # Swagger/OpenAPI support

# OCSP support
cryptography>=40.0.0  # Already included above
requests>=2.31.0

# Enhanced logging and monitoring
flask-limiter>=3.5.0  # Rate limiting
psutil>=5.9.0  # System monitoring

# Production Dependencies
gunicorn>=21.2.0  # WSGI server
flask-talisman>=1.1.0  # Security headers
python-dotenv>=1.0.0  # Environment variable management
redis>=4.5.0  # Redis client for rate limiting
psycopg2-binary>=2.9.0  # PostgreSQL adapter (already included above)

# Monitoring and metrics (optional)
# prometheus-flask-exporter>=0.23.0  # Prometheus metrics
# sentry-sdk[flask]>=1.32.0  # Error tracking

# Authentication and security
# Note: secrets, hashlib, and uuid are built-in Python modules

# MFA (Multi-Factor Authentication) support
pyotp>=2.9.0  # TOTP/HOTP implementation
qrcode>=7.4.2  # QR code generation
Pillow>=10.0.0  # Image processing for QR codes
